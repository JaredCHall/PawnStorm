{
  "imports": {
    "BitChess/": "./src/"
  },
  "tasks": {
    "play": "deno run --allow-run bitchess.ts",
    "perft": "deno run --allow-read perft.ts",
    "postinstall": "cp git-hooks/* .git/hooks",
    "test": "deno test --allow-run --allow-read",
    "test-unit": "deno task test -q tests/Unit",
    "test-integration": "deno task test tests/Integration",
    "test-coverage": "rm -r cov_profile && mkdir -p cov_profile && deno task test-unit --coverage=cov_profile && deno coverage cov_profile | tee tests/coverage-report.txt",
    "test-coverage-html": "deno task test-coverage && deno coverage cov_profile --html && xdg-open cov_profile/html/index.html"
  }
}